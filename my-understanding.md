React Assessment - Junior Software Developer (JSD13)

ตอนแรกที่เริ่มทำ ผมคิดว่าถ้าเขียนทุกอย่างไว้ในไฟล์เดียวมันจะยุ่งมาก เลยลองแบ่งเป็นชิ้นเล็กๆ ตามหน้าที่ของมัน เหมือนแยกเป็นชิ้นส่วนเลโก้ที่พอเอามาต่อกันแล้วเป็นหน้าเว็บทั้งหน้า
MainLayout.jsx — เป็นโครงหลักที่ครอบทุกหน้าไว้ มี Navbar อยู่ด้านบนตลอด
Navbar.jsx — แถบเมนูไว้กดสลับไปหน้า Home กับหน้า Owner
01_Header.jsx — ส่วนหัวข้อบนสุด เปลี่ยนข้อความตามโหมดที่เลือก
02_NavButtons.jsx — ปุ่มไว้สลับโหมด User กับ Admin
03_Display.jsx — คุมว่าจะโชว์ฟอร์มกรอกข้อมูล หรือโชว์ตาราง
Table.jsx — ตารางแสดงรายชื่อ ใช้ได้ทั้งฝั่ง user กับ admin แค่ admin จะมีปุ่มลบโผล่มา

ที่แบ่งแบบนี้เพราะ:

ใช้ซ้ำได้ — อย่าง Table.jsx ไม่ต้องเขียนสองรอบ ใช้ตัวเดียวกันแต่ปรับนิดหน่อยตามโหมด
แก้ง่าย — ถ้ามีบั๊กตรงไหน รู้เลยว่าต้องไปแก้ไฟล์ไหน ไม่ต้องงมทั้งโปรเจกต์
แยกส่วนแสดงผลกับส่วนคิดคำนวณออกจากกัน — ทำให้อ่านโค้ดง่ายขึ้นเยอะ
สร้าง state variable อะไรบ้าง และทำไมถึงสร้างขึ้นมา

ในหน้า Home.jsx มี state 3 ตัวนี้

sector — ไว้จำว่าตอนนี้อยู่โหมดไหน default, user, admin เพื่อให้หน้าจอโชว์อะไรถูกต้องตามโหมด
members — เก็บรายชื่อสมาชิกที่ดึงมาจาก API พอมีการเพิ่ม/ลบ ตัวนี้จะเปลี่ยนค่า แล้วตารางก็จะอัปเดตให้เองอัตโนมัติ
formData — ไว้เก็บค่าที่พิมพ์ในฟอร์ม ชื่อ, นามสกุล, ตำแหน่ง ก่อนจะกด Save ส่งไป API เรียกว่าทำ Controlled Component คือให้ React เป็นคนคุมค่าของ input แทนที่จะปล่อยให้ browser จัดการเอง
จัดการ state เหล่านี้ยังไง ใช้วิธีส่ง Props หรือใช้ React Context และทำไมถึงเลือกวิธีนั้น

เลือกใช้วิธี ส่ง Props ธรรมดา ไม่ได้ใช้ Context เพราะ

โครงสร้างหน้าเว็บยังไม่ลึกมาก แค่ 1-2 ชั้น Home จะมีการส่งลงไปหา Header, NavButtons, Display, Table
ส่ง Props ตรงๆ ทำให้เห็นชัดว่าข้อมูลวิ่งจากไหนไปไหน ตามได้ง่าย
ยังไม่ถึงจุดที่มีปัญหา Prop Drilling ส่ง props ลึกจนน่ารำคาญของจริงถ้าใช้ Context ตอนนี้เลยรู้สึกว่าจะเกินความจำเป็น เพิ่มความซับซ้อนโดยไม่จำเป็น
อธิบายว่าใช้ useEffect hook ยังไง และทำไมถึงต้องใช้
javascript
useEffect(() => {
  fetchMembers();
}, []);

ใช้ useEffect เพราะอยากให้พอหน้าเว็บโหลดเสร็จปุ๊บ ให้ไปดึงข้อมูลสมาชิกจาก API มาโชว์เลย ใส่ [] ว่างๆ ไว้ท้าย เพื่อบอก React ว่า "รันแค่ครั้งเดียวพอตอน component เพิ่งโผล่มาถ้าไม่ใส่ตรงนี้ไว้ อาจจะเกิดปัญหาดึงข้อมูลซ้ำไปเรื่อยๆ ไม่หยุด

อธิบายว่าสามารถใช้ fetch() โดยไม่ผ่าน useEffect ได้ไหม เพราะอะไร และใช้ในกรณีไหน

ได้ แต่ต้องเรียกตอนมี event เกิดขึ้นเท่านั้น เช่น:

กดปุ่ม Save → ยิง POST ไปเพิ่มข้อมูล
กดปุ่ม Delete → ยิง DELETE ไปลบข้อมูล

แต่ห้ามเขียน fetch() ลอยๆ ไว้ในตัว component ตรงๆ เด็ดขาด เพราะทุกครั้งที่ state เปลี่ยน component จะ render ใหม่ ถ้า fetch ไม่ได้อยู่ใน useEffect หรือ event handler มันจะยิงซ้ำไปเรื่อยๆ ไม่มีที่สิ้นสุด จนหน้าเว็บค้างหรือ server ล่มได้

อธิบายว่าการใช้ fetch() ควรเป็นแบบ synchronous หรือ asynchronous JavaScript เพราะอะไร

ต้องเป็น async เท่านั้น เพราะว่า

การเรียก API ผ่านเน็ตใช้เวลาไม่แน่นอน บางทีเร็ว บางทีช้า
JavaScript ทำงานทีละอย่างsingle-threaded ถ้าใช้แบบ sync รอจนกว่าจะเสร็จ หน้าเว็บจะค้างทันที กดอะไรไม่ได้เลยจนกว่าจะโหลดเสร็จ
ใช้ async ทำให้ browser ไปทำงานอื่นต่อได้ระหว่างรอ พอข้อมูลมาถึงค่อยกลับมาอัปเดตหน้าจอ ผู้ใช้เลยไม่รู้สึกว่าเว็บค้าง
บันทึกอื่นๆ เกี่ยวกับ React และการพัฒนา Frontend Web
Virtual DOM — React ไม่ได้ render ใหม่ทั้งหน้าทุกครั้งที่ state เปลี่ยน แต่จะเทียบก่อนว่าอะไรเปลี่ยนบ้าง แล้วอัปเดตเฉพาะจุดนั้น ทำให้เร็วขึ้นเยอะ
react-router-dom — ช่วยให้เปลี่ยนหน้าได้โดยไม่ต้องโหลดหน้าใหม่ทั้งหมด รู้สึกลื่นเหมือนแอป ไม่ใช่เว็บที่ต้องรีเฟรชตลอด
ข้อมูลไหลทางเดียว — ข้อมูลใน React จะไหลจาก parent ไปหา child เสมอ ไม่มีทางย้อนกลับ ทำให้เวลาบั๊กเกิดขึ้น จะตามหาจุดที่ผิดได้ง่ายกว่า